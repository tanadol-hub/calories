name: calories
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  # แก้ไขจาก 3.10.7 เป็น >=3.0.0 <4.0.0 เพื่อให้รองรับ Flutter 3.10 ขึ้นไปจนถึงปัจจุบัน
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  audioplayers: ^6.5.1
  image_picker: ^1.2.1
  http: ^1.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # ปรับเป็นเวอร์ชัน 4.0.0 หรือ 5.0.0 เพื่อให้เข้ากันได้กับ SDK ส่วนใหญ่
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/
    - assets/images/